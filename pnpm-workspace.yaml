packages:
  - frontend

allowBuilds:
  sharp: true
  unrs-resolver: true

# Next.js image optimisation and its resolver require native build steps.
# Keeping this explicit makes CI and local installs deterministic with pnpm 11+.
onlyBuiltDependencies:
  - sharp
  - unrs-resolver
