# RB FIAMM source-content wave 8

## Published only as noindex previews

| 1C external ID | MPN | Technical source | Legacy image result |
| --- | --- | --- | --- |
| `КА-00003136` | `4SLA150` | FIAMM-branded datasheet | image label visibly reads `4 SLA 150` |
| `КА-00004091` | `12FIT180` | official FIAMM product page | front label visibly reads `12 FIT 180` |
| `ФР-00001513` | `FG21803` | FIAMM datasheet | front label visibly reads `FG 21803`, `12 V`, `18 Ah` |

All three records passed the source-backed draft gate, then the noindex preview
gate, then the SHA-256 and visual-media gates. The images are from the
company-owned Bitrix upload archive, not a third-party shop.

## Safety invariants verified

- exactly 3 product pages published;
- exactly 3 verified images imported;
- all 3 `SiteUrl` records are `is_indexable = false`;
- no price, stock, offer or product-offer schema was created;
- browser check of `12FIT180` showed the exact image, `180 Ah (C10)` and
  `noindex, nofollow`.

After this wave RB contains 16 visible noindex previews and 13 verified
product images. This is evidence of the controlled publication path, not a
claim that the whole 7,630-product catalogue is complete or indexable.
