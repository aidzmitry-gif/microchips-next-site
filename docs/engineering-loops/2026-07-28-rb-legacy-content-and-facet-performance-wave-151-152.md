# Waves 151–152 — legacy-контент и производительность полного каталога

Дата: 2026-07-28

## Legacy-описания

- Детальный Bitrix snapshot run `742`: `1 569 / 1 569` строк имеют описание.
- `1 465` описаний точно сопоставлены с опубликованными safe-карточками через Bitrix ID.
- HTML преобразован в безопасный обычный текст; script/style удалены, сущности декодированы, пробелы нормализованы.
- Длина опубликованного preview-текста: от `441` до `1 485` символов.
- `104` hold/исключённых/непубличных записи не получили публичный текст.
- Ранее заполненные карточки не перезаписывались.

Статус `legacy_preview_applied` намеренно отличается от `applied`:
старый текст виден на noindex-карточке, но не считается подтверждённым
производителем и не увеличивает строгую SEO-готовность.

## Производительность фасетов

- Добавлен Redis-кэш набора facet records на 10 минут.
- Ключ содержит site, search, category, power-facet mode и версию каталога.
- `SiteContentChanged` увеличивает версию, поэтому обновлённые данные не используют старый кэш.

Локальное измерение `/api/v1/sites/microchips-by/catalog/products?per_page=24`:

| Режим | Время |
|---|---:|
| холодный | 1 452 ms |
| тёплый №1 | 140 ms |
| тёплый №2 | 146 ms |

Повторная выдача стала примерно в 10 раз быстрее при неизменных `16 414`
листинговых карточках.

## Проверка

- Preview descriptions: `1 465`, индексируемых страниц `0`.
- Manufacturer-verified descriptions created by this wave: `0`.
- Сквозная карточка `/catalog/industrial-batteries/batteries-industrial/legacy-bitrix-741`: HTTP 200, описание `1 035` символов, placeholder отсутствует, `isIndexable=false`.
- Повторный dry-run описаний: `0 selected / 0 remaining`.
- Повторный dry-run изображений: `0 selected / 0 remaining`.
- Ошибочный partial-upsert был полностью откачен транзакцией; реализация заменена на адресные UPDATE без вставки Product.

Следующий цикл: построить новую full-baseline очередь малоценных карточек, отделяя legacy-preview от проверенного контента, и начать массовое подтверждение точных товарных семейств.
