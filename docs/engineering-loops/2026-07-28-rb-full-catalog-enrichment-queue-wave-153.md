# Wave 153 — очередь строгого обогащения полного каталога

Дата: 2026-07-28

## Результат

- Генератор очереди получил режим `--published-only`; hold-черновики не попадают в исследовательский бюджет витрины.
- Полный набор обрабатывается чанками по `500` Eloquent-записей вместо загрузки всего графа в память.
- При штатном memory limit построена детерминированная очередь из `569` карточек для достижения первого порога `4%` строго content-complete товаров.
- Текущая строгая база: `88`; целевой порог: `657`; разница: `569`.

Legacy-preview описание или изображение намеренно не считается строгой
готовностью: оно помогает посетителю на noindex-странице, но карточка остаётся
в очереди до подтверждения источника/модели.

## Распределение очереди

| Категория | Карточек |
|---|---:|
| Аккумуляторы для ИБП | 434 |
| Аккумуляторные элементы | 35 |
| Батарейки | 33 |
| Блоки и источники питания | 23 |
| Зарядные устройства | 21 |
| ИБП | 14 |
| Прочие приоритетные группы | 9 |

## Артефакты

- `docs/audits/generated/rb-full-catalog-enrichment-wave153.csv`
- `docs/audits/generated/rb-full-catalog-enrichment-wave153.summary.json`

## Проверка

- Старый all-at-once режим действительно превышал обычный лимит памяти на полном каталоге; данные не изменялись.
- Временный контрольный расчёт при 512 MB: `569` строк.
- Chunked расчёт при штатном лимите: `569` строк.
- SHA-256 обоих CSV полностью совпал: `A690832BF00C33FBB495356C395E0132B0CB65126235822F8B4F41134EE69316`.
- `remaining_gap_after_queue=0` для целевого порога 4%.

Следующий цикл: сначала пройти позиции с одним пробелом (проверенное описание уже есть, требуется только точное изображение), затем исследовать official-source контент для карточек с двумя пробелами.
